{
  "emeraldwalk.runonsave": {
    "commands": [
      {
        // Run whenever any file is saved
        "match": ".*",
        "cmd": "git add --all && git commit -m \"Auto-commit: saved changes\" && git push"
      }
    ]
  }
}

